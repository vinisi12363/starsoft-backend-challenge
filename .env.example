# Database (PostgreSQL)
DATABASE_URL="postgresql://starsoft:starsoft123@localhost:5433/cinema_tickets?schema=public"

# Redis (Cache & Locks)
# Running locally via Docker Compose, Redis is exposed on port 6380
REDIS_HOST=localhost
REDIS_PORT=6380

# Messaging (Kafka)
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=cinema-api-client
KAFKA_GROUP_ID=cinema-api-group

# Application
PORT=3000
NODE_ENV=development
